#!/usr/bin/python

#script to get the models
#not working nao

#import os

#model_url = "http://dl.dropbox.com/u/2701879/models"

#print("Getting file list...")
#os.system("wget %s/list.txt -q -O list.txt" % model_url)

#models = open('list.txt').readlines()

#for model in models:
#	print("Getting %s ..." % model)
#	os.system( "wget %s/%s.obj -q -O %s" % (model_url, model, model) )
#	os.system( "wget %s/%s.mtl -q -O %s" % (model_url, model, model) )

